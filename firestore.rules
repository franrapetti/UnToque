rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow authenticated admin access to all collections
    // TODO: Add proper Firebase Auth and tighten these rules
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
